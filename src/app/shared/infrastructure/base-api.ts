/**
 * Abstract base class for API services.
 * Provides a common structure for all API-related services.
 */
export abstract class BaseApi {
  // No methods defined; subclasses will compose their own endpoint instances.
}
