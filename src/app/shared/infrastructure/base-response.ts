/**
 * Abstract base response interface for API responses.
 */
export interface BaseResponse {
}

/**
 * Abstract base resource interface for API resources.
 */
export interface BaseResource {
  id: number;
}
